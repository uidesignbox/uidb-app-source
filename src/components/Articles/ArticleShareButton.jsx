import React from 'react';

const ArticleShareButton = (props) => (
   <button className="footer-share__button" onClick={props.openModal} >Subscribe here</button>
);

export default ArticleShareButton;
