import React from 'react';
import '../../sass/layout-structure.scss';

const OneThirdCol = ({children}) => (
   <div className="col-one-third">
      {children}
   </div>
);

export default OneThirdCol;
